# ZCORIQ QA Report — 2026-05-02

End-to-end functional QA of the plan-proposal workflow + adjacent fixes
shipped today. Driven via Claude in Chrome against `localhost:3000`,
with `kmvipin@gmail.com` (Sudev Vipin, bootstrap platform admin) and
`test_admin@bloomiq.local` (Test Platform Admin, second admin created
specifically to exercise true two-eyes flow).

## Summary

| # | Test | Status | Notes |
|---|---|---|---|
| A | Platform-admin login redirect | PASS | Lands on `/admin/onboard-school` correctly |
| B | Admin dashboard / team quick-check | SKIPPED | Out of today's scope; not yet exercised |
| C | Clone form pre-fill from parent | PASS | Tier locked, Display name auto-suffixed `(variant)`, Blurb copied, Slug deliberately empty (must be new) |
| D | Side-by-side diff view | PASS | Slug renders as a real change for `kind='create'` (not "Locked"); "Show identical fields" toggle quarantines unchanged content into separate sections; identical features now show on **both** columns side-by-side |
| E | Approve as-is end-to-end | PASS | New SKU `Premium Plus Quarterly (QA test)` lands in `/admin/plans` PREMIUM PLUS section with `₹549 / 90d` exactly as approved. `bootstrap_self_approve = false` (correct for 2-admin mode). |
| F | Edit-and-approve | PASS | Approver bumped price `₹325 → ₹349` mid-approval; resulting plan row stamped `₹349`; proposal record stamped `approved_with_edits = true`; queue card audit line shows `(with edits)` suffix. |
| G | Reject with reason | PASS | Modal copy *"The creator will see your reason. Be specific about what needs to change."*; reject button disabled until reason entered; rejection reason preserved verbatim on the Rejected card. |
| H | Withdraw (creator-only) | PASS | URL redirects to `?ok=withdrawn`. Proposal removed from "My drafts". |
| I | Existing-edit flow (`kind='edit'`) | PASS | Header reads "Edit proposal — Premium Plus Annual"; left column "LIVE: PREMIUM PLUS ANNUAL"; Slug + Tier rows show 🔒 LOCKED with "(immutable — won't apply)" placeholder — exactly the inverse of the create flow. |
| J | Pre-test track-time checkbox | PASS | Quiz `5QQ8YN` ("Practice: Photosynthesis basics", 6 questions / 9 min). Pre-test screen renders before the timer starts; checkbox unchecked by default; explanatory copy mentions Premium Plus cohort comparison. |
| K | Premium Plus daily-cap banner | PASS | `premiumplus.student@example.com` dashboard renders without the misleading "0 of 3 free attempts left today" banner. Migration 44 (CASE statement fix + `is_active_paid` predicate) confirmed working. |

**Overall: 10 of 11 planned tests passed. 1 deliberately skipped. Zero
unresolved blockers.**

## Bugs surfaced and resolved during QA

### Schema cache miss — migration 32 not applied
First Approve attempt failed with *"Could not find the 'approved_at'
column of 'plans' in the schema cache"*. Migration 32 (which adds
`status`, `created_by`, `approved_by`, `approved_at`, `effective_from`,
`effective_to`, plus `plans_two_eyes` CHECK) had not been run on the
database despite being on `main`. Resolved live by running migration 32
in the Supabase SQL Editor. Approve flow then completed end-to-end on
the next attempt.

The proposal stayed in `status='open'` after the failure (no
half-applied state) — the approve route's error handling correctly
aborted before stamping. No rework was needed after running the
migration.

## Bugs / findings parked as tasks

| Task | Title | Status |
|---|---|---|
| #9 | Wire creator "Edit my draft" UI to PATCH endpoint | DONE — replaced 404 link with inline edit form (creator mode) |
| #10 | Platform Admin tab missing from /login | NOT-A-BUG — intentional per code comment |
| #11 | No UI surface for withdrawn proposals | DONE — added "Withdrawn" tab next to "Rejected" |
| #12 | Add hidden `/staff` route for platform-admin login | OPEN — recommended path until intranet ships |

## Two-eyes flow verified

Created `test_admin@bloomiq.local` (script: `scripts/create-test-platform-admin.js`).
With two platform admins in the DB:

- A creator could not approve their own proposal — the approver buttons
  disappeared and only Withdraw + Edit draft remained for the creator's
  own view of an open proposal.
- The other admin saw Approve as-is / Edit and approve / Reject,
  exactly as designed.
- `bootstrap_self_approve` was correctly `false` on the resulting plan
  row + proposal record, confirming the bootstrap exception is only
  applied when there's truly only one admin.

## Diff view rendering — explicit checks

- Empty diff (no changes): summary banner reads "No changes". Tested.
- 4-field diff on a clone: banner reads "4 changes · 4 fields"; per-section badges count correctly. Tested.
- Show-identical-fields toggle: scalar identical rows fade to 55% opacity; identical features are quarantined in a `<details>` collapsible block titled "Unchanged on both sides (N)" inside each side's column. Tested.
- `kind='create'` vs `kind='edit'` semantics: slug + tier are LOCKED on edit, freely editable on create. Tested both directions.

## Premium Plus daily-cap fix verification

Migration 44 reshaped the broken CASE-statement logic in BOTH
`attempts_remaining_today()` (read-side helper) AND `check_attempt_quota`
trigger (write-side gate). After the migration:

- The Premium Plus student's `/student` dashboard loaded with NO
  "0 of 3 free attempts left today" banner anywhere.
- The pre-test screen (Test J) successfully started a fresh quiz
  attempt — meaning the trigger no longer rejects Premium Plus inserts.

## What was NOT tested today

These are not failures, just out of scope:

- `/admin/dashboard` and `/admin/team` UI walkthrough.
- Razorpay checkout against the new `Premium Plus Quarterly (QA test)` SKU (no `razorpay_plan_id` set yet — the column exists per migration 43, but until that field is populated, the SKU is admin-visible but not purchasable).
- Email reminders before subscription expiry (none exist; flagged in earlier session).
- Quiz submission path — only the pre-test screen was loaded; we didn't actually take the quiz to verify per-question time tracking writes through to `attempt_answers.time_taken_ms`.
- Concurrent edit-proposal handling on the same target plan (`proposals_one_open_edit_per_target` unique index — should reject second draft on same plan).
- Rejection-then-retry (creator sees the reason and resubmits a new proposal).

## Recommended next session

1. **Ship `/staff` route + 2FA mandatory for platform_admin** (security hardening before launch).
2. **Razorpay sync on plan approval** — wire up `razorpay_plan_id` field auto-population so new SKUs are immediately purchasable.
3. **Subscription-expiry reminder emails** — T-30 / T-7 / T-0 / T+3 cadence, also a school-admin banner on `/school` (currently only independent students see expiry warnings).
4. **Functional QA round 2** — drive the deferred tests (B, the un-tested edge cases listed above).

---

Generated 2026-05-02 from a Chrome-driven QA session against `localhost:3000`.
